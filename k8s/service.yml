apiVersion: v1
kind: Service
metadata:
  name: hgkrwi
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "hgkrwi"
    port: 8080
  selector:
    app: hgkrwi
